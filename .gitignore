# Локальные рабочие копии Claude — не должны быть в репозитории
**/.claude/
__pycache__/
*.pyc

# Локальные черновики и бэкапы — не сайт для Pages
Local/

# Локальная копия «Мёртвые души» не должна попадать на GitHub
**/Мёртвые души/

cloudflare-worker/.wrangler/
