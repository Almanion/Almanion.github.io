# На GitHub — только файлы сайта в корне репозитория.
# Локально рядом могут лежать и не попадают в Git:
Local/
cloudflare-worker/

# Если папка docs/ остаётся только как локальный мусор/worktrees — не коммитить
/docs/

# Артефакты
**/.claude/
__pycache__/
*.pyc
**/Мёртвые души/
